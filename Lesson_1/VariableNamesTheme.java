public class VariableNamesTheme {
    public static void main(String[] args) {
        System.out.println("1.Разные переменные");

        // Цифра
        int digit = 1;
        System.out.println("\n\nЦифра = " + digit);

        // Сумма цифр
        int digitSum = 5;
        System.out.println("\nСумма цифр = " + digitSum);

        // Произведение цифр 
        int digitProduct = 5;
        System.out.println("\nПроизведение цифр = " + digitProduct);

        // Максимальное число
        int maxNumber = 100;
        System.out.println("\nМаксимальное число = " + maxNumber);

        // Количество десятков
        int tenCount = 10;
        System.out.println("\nКоличество десятков = " + tenCount);

        // Вес собаки
        int dogWeight = 25;
        System.out.println("\nВес собаки = " + dogWeight);

        // Исходное число
        int initialNumber = 5;
        System.out.println("\nИсходное число = " + initialNumber);

        // Процент по вкладу
        int depositRate = 3;
        System.out.println("\nПроцент по вкладу = " + depositRate);

        // Переменная хранит символ &
        String sign = "&";
        System.out.println("\nПеременная хранит символ = " + sign);

        // Код ошибки
        String errorCode = "NullPointerException";
        System.out.println("\nКод ошибки = " + errorCode);

        // Тип сообщения
        String messageType = "Mail";
        System.out.println("\nТип сообщения = " + messageType);

        // Число нулей
        int zeroCount = 00;
        System.out.println("\nЧисло нулей = " + zeroCount);

        // Уникальное число
        int uniqNumber = 333;
        System.out.println("\nУникальное число = " + uniqNumber);

        // Cлучайное число
        int randomNumber = 1394;
        System.out.println("\nCлучайное число = " + randomNumber);

        // Математическое выражение
        String mathExpression = "x + y";
        System.out.println("\nМатематическое выражение = " + mathExpression);

        // Cчет в игре
        int gameScore = 100;
        System.out.println("\nCчет в игре = " + gameScore);

        // Максимальная длина
        int maxLength = 2000;
        System.out.println("\nМаксимальная длина = " + maxLength);

        // Пункт меню
        String menuItem = "Soup";
        System.out.println("\nПункт меню = " + menuItem);

        // Стоимость кофе
        int coffeePrice = 150;
        System.out.println("\nСтоимость кофе = " + coffeePrice);

        // Начальная дата
        String startDate = "20.01.2022";
        System.out.println("\nНачальная дата = " + startDate);

        // Окончание диапазона
        int rangeEnd = 99;
        System.out.println("\nОкончание диапазона = " + rangeEnd);

        // Имя работника месяца
        String employeeOfMonth = "Carl";
        System.out.println("\nИмя работника месяца = " + employeeOfMonth);

        // Название электронной книги\
        String ebookTitle = "Amazon";
        System.out.println("\nНазвание электронной книги = " + ebookTitle);

        // Размер
        int size = 52;
        System.out.println("\nРазмер = " + size);

        // Вместимость
        int capacity = 100;
        System.out.println("\nВместимость = " + capacity);

        // Счетчик
        int counter = 1;
        System.out.println("\nСчетчик = " + counter);

        // Путь до файла
        String filePath = "D:/utils/";
        System.out.println("\nПуть до файла = " + filePath);

        // Количество чисел в каждой строке
        int numCountPerLine = 10;
        System.out.println("\nКоличество чисел в каждой строке = " + numCountPerLine);

        System.out.println("\n\n2.Boolean-переменные");

        // Сотни равны?
        boolean isEqualHundreds = true;
        System.out.println("\n\nСотни равны? = " + isEqualHundreds);

        // Компьютер включен?
        boolean isComputerOn = false;
        System.out.println("\nКомпьютер включен? = " + isComputerOn);

        // Есть равные цифры?
        boolean hasEqualNumbers = true;
        System.out.println("\nЕсть равные цифры? = " + hasEqualNumbers);

        // Создано?
        boolean isCreated = false;
        System.out.println("\nСоздано? = " + isCreated);

        // Пустое?
        boolean isEmpty = true;
        System.out.println("\nПустое? = " + isEmpty);

        // Активное?
        boolean active = false;
        System.out.println("\nАктивное? = " + active);

        // Новое?
        boolean isNew = true;
        System.out.println("\nНовое? = " + isNew);

        // Электронная почта действительная?
        boolean isValidEmail = true;
        System.out.println("\nЭлектронная почта действительная? = " + isValidEmail);

        // Имеются уникальные строки?
        boolean hasUniqStrings = true;
        System.out.println("\nИмеются уникальные строки? = " + hasUniqStrings);

        System.out.println("\n\n3.Аббревиатуры ");

        // Cтарый universally unique identifier
        String oldUuid = "61f0c404-5cb3-11e7-907b-a6006ad3dba0";
        System.out.println("\n\nCтарый universally unique identifier = " + oldUuid);

        // Производитель оперативной памяти
        String ramVendor = "Asus";
        System.out.println("\nПроизводитель оперативной памяти = " + ramVendor);

        // Емкость жесткого диска
        int hddCapacity = 256;
        System.out.println("\nЕмкость жесткого диска = " + hddCapacity);

        // Протокол передачи гипертекста
        String http = "http";
        System.out.println("\nПротокол передачи гипертекста = " + http);

        // Сокращенный uniform resource locator
        String shortUrl = "shortUrl";
        System.out.println("\nСокращенный uniform resource locator = " + shortUrl);

        // Новый идентификатор клиента
        int newClientId = 12;
        System.out.println("\nНовый идентификатор клиента = " + newClientId);

        // American standard code for information interchange
        String ascii = "ascii";
        System.out.println("\nAmerican standard code for information interchange = " + ascii);
    }
}